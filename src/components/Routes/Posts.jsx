import styled from "styled-components";

function Posts() {
  return(
    <PostContainer>
      
    </PostContainer>
  );
}

export default Posts;

const PostContainer = styled.section`
  display: flex;
`